spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_docker_database
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true